<swiper autoplay="{{true}}" interval="{{2000}}" duration="{{1000}}" vertical="{{true}}" circular="{{true}}">
  <block wx:for="{{imgUrls}}">
    <swiper-item>
      <image src="{{item}}" class="slide-item" />
    </swiper-item>
  </block>
</swiper>